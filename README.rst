
Django-rulebuilder allows for conditional expressions to be created and defined through use of JSON Schema. Once and expression is defined it can evaluate a given context and return the truth value of the evaluation.
